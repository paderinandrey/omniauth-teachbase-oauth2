RSpec.describe Omniauth::Teachbase::Oauth2 do
  it "has a version number" do
    expect(Omniauth::Teachbase::Oauth2::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
